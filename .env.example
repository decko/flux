# Flux environment variables
# Copy to .env and fill in your values.

# Server
PORT=8080

# Database
DATABASE_DRIVER=sqlite
DATABASE_DSN=./flux.db

# Auth (generate with: openssl rand -base64 32)
FLUX_JWT_SECRET=change-me

# GitHub integration (optional)
GITHUB_TOKEN=
